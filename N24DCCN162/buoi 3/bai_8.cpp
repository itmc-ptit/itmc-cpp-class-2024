#include <iostream>
#include <cmath>
using namespace std;
int main() {
    float a,b,e ;
    cout << "Nhap 2 so thuc " << endl;
    cin >> a >> b ;
     double c = floor (a);
     double d = floor (b);
     e = c + d;
     cout <<"Tong phan nguyen 2 so la : " << e;
     return 0;

}
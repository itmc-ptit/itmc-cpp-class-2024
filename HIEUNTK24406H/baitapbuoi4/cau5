#include<iostream>
#include <algorithm>
using namespace std;
int main()
{
    int a , b , c , d;
    cout << "nhap a , b , c , d : ";
    cin >> a >> b >> c >> d;
    cout << "vay so lon nhat la " << max(max(a,b) , max(c,d));
    system ("pause");
}
#include <iostream>
using namespace std;

int main() {
    float a, b;
    cout << "nhap so a va b ";
    cin >> a >> b;

    if (a == 0) {
        cout << (b == 0  "ptr co vo so nghiem" : "ptr vo nghiem") << endl;
    } else {
        cout << "nghiem cua ptr la: x = " << -b / a;
    }

    return 0;
}

#include <iostream>
using namespace std;
int main () {
    int a, b;
cout << "nhap a va b: ";
    cin >> a >> b;
cout << "gia tri sau ban dau: a = " << a << ", b = " << b << endl;
swap (a,b);

    cout << "gia tri sau khi doi: a = " << a << ", b = " << b << endl;

    return 0;
}
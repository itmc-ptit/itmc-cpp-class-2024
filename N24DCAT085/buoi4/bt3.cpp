#include <iostream>
#include <cmath>
using namespace std;
int main (){
    int n;
    cin >>n;
    int sqrtn=sqrt(n);
    if (sqrtn*sqrtn==n) {cout<< "do la so chinh phuon ";}
    else {cout<<"do k pk so chinh phuong";}
}